package org.example;

public interface Figura {
    double area();
}


//a) Deberia ser una interfaz

/*
b)
for (Figura f : figuras) {
    total += f.area();
}
Este es un polimorfismo de subtipado (o polimorfismo de inclusión), que ocurre cuando una referencia de una clase base (o interfaz) puede apuntar a instancias de sus subclases y llamar a métodos sobrescritos.
 */


//c) Foto

//d) intellij